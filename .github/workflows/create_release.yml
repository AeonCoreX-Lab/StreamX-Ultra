name: Create New Release

on:
  push:
    tags:
      - 'v*.*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
      issues: read

    steps:
      # --- ধাপ ১: রিপোজিটরির কোড আনা ---
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # ইতিহাস চেক করার জন্য সম্পূর্ণ হিস্ট্রি আনা প্রয়োজন

      # --- ধাপ ২: JDK 17 সেটআপ করা ---
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # --- ধাপ ৩: GitHub Secrets থেকে Keystore ডিকোড করা ---
      - name: Decode Keystore
        run: |
          echo "${{ secrets.RELEASE_KEYSTORE }}" | base64 --decode > ${{ github.workspace }}/release.keystore

      # --- ধাপ ৪: google-services.json তৈরি করা ---
      - name: Create google-services.json
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          echo $GOOGLE_SERVICES_JSON | base64 -d > app/google-services.json

      # --- ধাপ ৫: gradlew পারমিশন ---
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # --- ধাপ ৬: সাইন করা রিলিজ APK তৈরি করা ---
      - name: Build Signed Release APK
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
          RELEASE_KEYSTORE_FILE: ${{ github.workspace }}/release.keystore
          RELEASE_KEYSTORE_PASSWORD: ${{ secrets.RELEASE_KEYSTORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          ./gradlew assembleRelease \
            -PRELEASE_KEYSTORE_FILE=${{ env.RELEASE_KEYSTORE_FILE }} \
            -PRELEASE_KEYSTORE_PASSWORD=${{ env.RELEASE_KEYSTORE_PASSWORD }} \
            -PRELEASE_KEY_ALIAS=${{ env.RELEASE_KEY_ALIAS }} \
            -PRELEASE_KEY_PASSWORD=${{ env.RELEASE_KEY_PASSWORD }}

      # --- ধাপ ৭: APK Artifact আপলোড ---
      - name: Upload Release APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: StreamX-Ultra-Release-APK
          path: app/build/outputs/apk/release/*.apk

      # --- ধাপ ৮: StreamX Bot - অটোমেটিক রিলিজ নোট জেনারেট করা ---
      - name: "StreamX Bot: Generate Release Notes"
        id: build_changelog
        uses: mikepenz/release-changelog-builder-action@v4
        with:
          toTag: ${{ github.ref_name }}
          # Bot কনফিগারেশন: কমিট মেসেজ অনুযায়ী ক্যাটাগরি তৈরি হবে
          configurationJson: |
            {
              "template": "# 🚀 StreamX Ultra ${{ github.ref_name }}\n\n## 📋 Change Log\n#{{changelog}}\n\n---\n\n### 📦 Download Info\n- **Version**: ${{ github.ref_name }}\n- **Status**: Stable Release\n\n🤖 *Generated by StreamX Bot* | © AeonCoreX™",
              "categories": [
                {
                    "title": "## ✨ New Features",
                    "labels": ["feat", "feature", "new", "add"]
                },
                {
                    "title": "## 🐛 Bug Fixes",
                    "labels": ["fix", "bug", "hotfix", "patch"]
                },
                {
                    "title": "## 🎨 UI/UX Improvements",
                    "labels": ["ui", "design", "style", "ux"]
                },
                {
                    "title": "## ⚡ Performance & Refactoring",
                    "labels": ["perf", "refactor", "chore", "clean"]
                }
              ],
              "ignore_labels": ["release", "ignore", "ci"],
              "sort": "label"
            }
        env:
          GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }} # আপনার সেট করা টোকেন ব্যবহার করা হচ্ছে

      # --- ধাপ ৯: GitHub রিলিজ তৈরি করা ---
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: app/build/outputs/apk/release/*.apk
          name: "StreamX Ultra ${{ github.ref_name }}"
          tag_name: ${{ github.ref_name }}
          # Bot এর তৈরি করা নোট এখানে সেট করা হলো
          body: ${{ steps.build_changelog.outputs.changelog }}
        env:
          GITHUB_TOKEN: ${{ secrets.BOT_TOKEN }}