name: Create New Release

on:
  # v1.0, v1.1, v1.2.1 এই ধরনের ট্যাগ পুশ করলেই এই Workflow চলবে
  push:
    tags:
      - 'v*.*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    # রিলিজ তৈরি করার জন্য প্রয়োজনীয় 'write' পারমিশন
    permissions:
      contents: write

    steps:
      # ধাপ ১: রিপোজিটরির কোড আনা
      - name: Checkout repository
        uses: actions/checkout@v4

      # ধাপ ২: JDK 17 সেটআপ করা
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # ধাপ ৩: GitHub Secrets থেকে Keystore ডিকোড করা
      - name: Decode Keystore
        run: |
          echo "${{ secrets.RELEASE_KEYSTORE }}" | base64 --decode > ${{ github.workspace }}/release.keystore

      # ধাপ ৪: gradlew ফাইলকে এক্সিকিউট করার অনুমতি দেওয়া
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # ধাপ ৫: সাইন করা রিলিজ APK তৈরি করা
      - name: Build Signed Release APK
        env:
          # এনভায়রনমেন্ট ভেরিয়েবল হিসেবে তথ্য সেট করা
          RELEASE_KEYSTORE_FILE: ${{ github.workspace }}/release.keystore
          RELEASE_KEYSTORE_PASSWORD: ${{ secrets.RELEASE_KEYSTORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          # প্রজেক্ট প্রপার্টি হিসেবেও তথ্য পাস করা হচ্ছে (বুলেটপ্রুফ সমাধান)
          ./gradlew assembleRelease \
            -PRELEASE_KEYSTORE_FILE=${{ env.RELEASE_KEYSTORE_FILE }} \
            -PRELEASE_KEYSTORE_PASSWORD=${{ env.RELEASE_KEYSTORE_PASSWORD }} \
            -PRELEASE_KEY_ALIAS=${{ env.RELEASE_KEY_ALIAS }} \
            -PRELEASE_KEY_PASSWORD=${{ env.RELEASE_KEY_PASSWORD }}

      # ধাপ ৬ (ডিবাগিং): APK ফাইলটি কোথায় তৈরি হয়েছে তা প্রিন্ট করা
      - name: List Release Directory Files for Debugging
        run: ls -R app/build/outputs/apk/release

      # ধাপ ৭: রিলিজ APK ফাইলটিকে Artifact হিসেবে আপলোড করা
      - name: Upload Release APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: StreamX-Ultra-Release-APK
          path: app/build/outputs/apk/release/*.apk

      # ধাপ ৮: রিলিজ নোট তৈরি এবং সংস্করণ তথ্য সংগ্রহ করা (আপডেট করা হয়েছে)
      - name: Create Release Notes and Get Version Info
        id: create_notes
        run: |
          VERSION_NAME=$(grep 'versionName' app/build.gradle.kts | sed -n 's/.*versionName = "\(.*\)".*/\1/p')
          
          # নতুন রিলিজ নোট তৈরি করা হচ্ছে
          echo "🚀 StreamX Ultra v1.1 – The Ultimate Upgrade" > RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "Official Live TV & IPTV Platform by AeonCoreX™" >> RELEASE_NOTES.md
          echo "First Stable Public Release" >> RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "✨ What's New in v1.1" >> RELEASE_NOTES.md
          echo "----------------------" >> RELEASE_NOTES.md

          echo "🔐 Multi-Platform Authentication" >> RELEASE_NOTES.md
          echo "- Login with Google" >> RELEASE_NOTES.md
          echo "- Login with Facebook" >> RELEASE_NOTES.md
          echo "- Login with GitHub" >> RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "🎨 Ultimate Auth Screen UI" >> RELEASE_NOTES.md
          echo "- Fully redesigned Login & Sign Up screen" >> RELEASE_NOTES.md
          echo "- Professional, modern & clean UI" >> RELEASE_NOTES.md
          echo "- Optimized for all screen sizes" >> RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "▶️ Advanced Player Features" >> RELEASE_NOTES.md
          echo "- Rotation Control (Portrait / Landscape)" >> RELEASE_NOTES.md
          echo "- Double Tap to Seek (Forward / Backward 10s)" >> RELEASE_NOTES.md
          echo "- Live Data Usage Statistics" >> RELEASE_NOTES.md
          echo "- Real-time internet speed monitoring" >> RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "👤 Functional Account & Settings" >> RELEASE_NOTES.md
          echo "- Manage profile information" >> RELEASE_NOTES.md
          echo "- Change password securely" >> RELEASE_NOTES.md
          echo "- View connected login methods" >> RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "⚖️ Copyright & Disclaimer" >> RELEASE_NOTES.md
          echo "- Dedicated legal & disclaimer page added" >> RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "📦 Version Information" >> RELEASE_NOTES.md
          echo "- Version Name: v1.1" >> RELEASE_NOTES.md
          echo "- Version Code: 2" >> RELEASE_NOTES.md
          echo "- Release Type: First Stable Release" >> RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "📲 Download APK" >> RELEASE_NOTES.md
          echo "https://github.com/cybernahid-dev/StreamX-Ultra/releases/download/v1.1/app-release.apk" >> RELEASE_NOTES.md
          echo "" >> RELEASE_NOTES.md

          echo "© AeonCoreX™ – All Rights Reserved" >> RELEASE_NOTES.md
          
          # আউটপুট সেট করা যাতে পরের ধাপে ব্যবহার করা যায়
          echo "version_name=${VERSION_NAME}" >> $GITHUB_OUTPUT
          echo "notes_path=RELEASE_NOTES.md" >> $GITHUB_OUTPUT

      # ধাপ ৯: GitHub রিলিজ তৈরি এবং APK আপলোড করা
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          # ওয়াইল্ডকার্ড ব্যবহার করে APK ফাইল খুঁজে বের করা হচ্ছে
          files: app/build/outputs/apk/release/*.apk
          name: "Version ${{ steps.create_notes.outputs.version_name }}"
          tag_name: ${{ github.ref_name }}
          body_path: ${{ steps.create_notes.outputs.notes_path }}
