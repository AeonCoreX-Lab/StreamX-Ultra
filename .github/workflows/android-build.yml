name: StreamX-Ultra APK & Keys

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ধাপ ১: রিপোজিটরির কোড আনা
      - name: Checkout repository
        uses: actions/checkout@v4

      # ধাপ ২: JDK 17 সেটআপ করা
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      # --- নতুন ধাপ: google-services.json তৈরি করা ---
      - name: Create google-services.json
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          echo $GOOGLE_SERVICES_JSON | base64 -d > app/google-services.json

      # ধাপ ৩: Gradle Wrapper-কে এক্সিকিউট করার অনুমতি দেওয়া
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # ধাপ ৪: Gradle ব্যবহার করে ডিবাগ APK বিল্ড করা
      - name: Build Debug APK
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
        run: ./gradlew assembleDebug

      # ধাপ ৫: কী (SHA-1, SHA-256) প্রিন্ট করা
      - name: Generate and Print Signing Keys
        run: |
          echo "===================================================================="
          echo "🔑 GENERATING ALL SIGNING FINGERPRINTS..."
          echo "===================================================================="
          ./gradlew signingReport
          echo "===================================================================="

      # ধাপ ৬: APK ফাইল আপলোড করা
      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: StreamX-Ultra-Debug-APK
          path: app/build/outputs/apk/debug/app-debug.apk