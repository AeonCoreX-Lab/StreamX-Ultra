<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ইন্টারনেট ব্যবহারের অনুমতি -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- নোটিফিকেশন দেখানোর অনুমতি (ঐচ্ছিক কিন্তু ভালো) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StreamXUltra"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.StreamXUltra"
            
            <!-- =================== এই দুটি লাইন যোগ করা হয়েছে =================== -->
            <!-- PiP মোড সমর্থন করার জন্য এই লাইনটি অপরিহার্য -->
            android:supportsPictureInPicture="true"
            
            <!-- PiP মোডে যাওয়ার সময় অ্যাপ ক্র্যাশ হওয়া থেকে আটকাতে এই লাইনটি অপরিহার্য -->
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden|uiMode"
            <!-- ================================================================= -->
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>